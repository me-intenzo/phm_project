dataset: FD004

window_size: 40

batch_size: 256

epochs: 300

learning_rate: 0.001

optimizer: Adam

random_seed: 42

#device: cpu //use only if u wanna run on cpu or lack gpu, otherwise use cuda
device: cuda